# Data Folder

Use this folder for route inputs and working datasets.

- `raw/` is for source GPX and CSV files you want to keep around as inputs.
- Generated exports should go under [outputs/](../outputs/) instead of the repository root.